因为沟通能力比较薄弱需要加强锻炼并且将优秀的案例记录下来：

1、在面对复杂的情况时，需要将每一步的流程写出来 我感觉后续的分析步骤也有很大的可取之处；但是暂时还没有提炼
``` shell
1、全友对销售预测数据的使用方式
        设计数据：按方案更新时间进行覆盖更新。
        审核数据：只取首次提审时间对应的提审数据。
2、后端接口能力
        后端接口仅能获取当前设计方案和审核单的最新状态数据，无法获取历史状态数据。
3、数仓对外提供的数据
        数仓侧对设计方案和提审单均为按天增量更新的数据。
4、当前数据缺失情况
        设计方案：全友缺失的是方案最后一次修改时间在 2025-12-26 至 2026-03-04 期间的数据；该时间范围之外的修改会正常触发埋点。
        审核单：由于无法获取历史状态数据，因此无法还原历史上的首次提审时间。
5、当前可处理方案
        设计方案：可以通过重新调用接口获取当前最新状态的数据进行补充。
        审核单：首次提审的数据无法恢复；（只能类似设计方案的处理方式，获取最后一次修改时间在指定区间内的审核单数据进行补充。@林鸣(林鸣) 这个确认一下）
6、我的问题：接口的入参不仅仅包含方案ID，还需要其他参数。目前我这边无法确认完整的入参结构，因此接口调用这块可能需要你们来处理，我这边可以提供具体的方案ID列表
```

